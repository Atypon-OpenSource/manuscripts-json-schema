{
  "$id": "MPManuscriptNote.json",
  "title": "ManuscriptNote",
  "$mash": {
    "sources": [{ "$ref": "concrete/MPCommentAnnotation.json#" }],
    "with": {
      "properties": {
        "objectType": {
          "type": "string",
          "enum": ["MPManuscriptNote"]
        },
        "source": {
          "$ref": "enums.json#/definitions/NoteSource"
        },
        "displayName": {
          "type": "string",
          "description": "For storing the user name when the comment was created outside of Manuscripts"
        },
        "additionalProperties": false
      },
      "required": ["source"]
    }
  }
}
